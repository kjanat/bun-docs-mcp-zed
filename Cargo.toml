[package]
name = "bun-docs-mcp-zed"
version = "0.1.0"
edition = "2021"
description = "Bun Docs MCP extension for Zed"
license = "MIT"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
sha2 = "0.10"
zed_extension_api = "0.7.0"

[dev-dependencies]
tempfile = "3"

[profile.release]
opt-level = "z"
strip = true
lto = true
panic = "abort"
codegen-units = 1
