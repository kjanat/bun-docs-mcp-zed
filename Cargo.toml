[package]
name = "bun-docs-mcp-zed"
version = "0.1.0"
edition = "2021"
description = "Bun Docs MCP extension for Zed"
license = "MIT"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
zed_extension_api = "0.7.0"

[profile.release]
opt-level = "z"
strip = true
lto = true
panic = "abort"
codegen-units = 1
